<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <title>{{ SITE_NAME }}</title>
    <link href="{{ SITE_URL }}" rel="alternate"></link>
    <link href="{{ SITE_URL }}/atom.xml" rel="self"></link>
    <id>{{ SITE_URL }}</id>
    <updated>{{ NOW.isoformat() }}</updated>
    {% for post in posts %}
    <entry>
        <title>{{ post.title }} </title>
        <link href="{{ SITE_URL }}/{{ post.slug }}.html" rel="alternate"></link>
        <published>{{ post.date.isoformat() }}</published>
        <updated>{{ post.date.isoformat() }}</updated>
        <author>
            <name>{{ AUTHOR_NAME }}</name>
        </author>
        <id>tag:{{ SITE_HOSTNAME }},{{ post.date.strftime("%Y-%m-%d") }}:/{{ post.slug }}.html</id>
        <summary type="html"></summary>
        <content type="html"></content>
    </entry>
    {% endfor %}
</feed>